platform: linux

image_resource:
  type: registry-image
  source:
    repository: alpine
    tag: "3.20"

outputs:
  - name: out

params:
  MESSAGE: ((hello.myvaultsec))

run:
  path: sh
  args:
    - -ec
    - |
      if [ -n "$MESSAGE" ]; then
        echo "Hier könnte ihre Werbung stehen."
        echo "Erhalte jetzt für nur 4.99€ Spotify Premium."
      else
        echo "MESSAGE is empty" >&2
        exit 1
      fi

      date -u
      echo "OK" > out/result.txt
      ls -la out
