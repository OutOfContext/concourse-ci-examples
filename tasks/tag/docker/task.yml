platform: linux

image_resource:
  type: registry-image
  source:
    repository: alpine
    tag: "3.20"

inputs:
  - name: image

outputs:
  - name: image

run:
  path: sh
  args:
    - -ec
    - |
      ls -la