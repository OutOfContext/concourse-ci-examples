platform: linux

image_resource:
  type: registry-image
  source:
    repository: maven

inputs:
  - name: app

outputs:
  - name: build

run:
  path: sh
  args:
    - -ec
    - |
      ls -la
      cd app/backend
      ls -la
      mvn -B -DskipTests package
      cp target/*.jar ../build/app.jar
